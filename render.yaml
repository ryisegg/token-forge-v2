services:
  - type: web
    name: tokens-forge-gateway
    runtime: docker
    repo: https://github.com/ryisegg/tokens-forge-gateway
    plan: free
    region: oregon
    branch: main
    autoDeploy: true
    dockerfilePath: ./Dockerfile
    dockerContext: .
    healthCheckPath: /api/status
    envVars:
      - key: PORT
        value: 10000
      - key: SQL_DSN
        fromDatabase:
          name: tokens-forge-gateway-db
          property: connectionString
      - key: LOG_SQL_DSN
        fromDatabase:
          name: tokens-forge-gateway-db
          property: connectionString
      - key: SESSION_SECRET
        generateValue: true
      - key: CRYPTO_SECRET
        generateValue: true
      - key: NODE_NAME
        value: tokens-forge-gateway-render
      - key: ERROR_LOG_ENABLED
        value: "true"
      - key: BATCH_UPDATE_ENABLED
        value: "true"
      - key: MEMORY_CACHE_ENABLED
        value: "true"
      - key: TZ
        value: UTC

databases:
  - name: tokens-forge-gateway-db
    databaseName: tokens_forge_gateway
    user: tokens_forge_gateway
    plan: free
    postgresMajorVersion: "15"
    ipAllowList: []
